<p>
  <strong>Название:</strong>
  <%= @route.name %>
</p>

<p>
  <strong>Поезд на данном маршруте:</strong>
  <%= @route.train.name if !@route.train.nil? %>
  <%= "поезда нет" if @route.train.nil? %>
</p>

<p>
  <strong>Станции которые входят в данный маршрут:</strong>
  <%= @route.railway_stations.map(&:title) if @route.railway_stations.any? %>
  <%= "станций нет" if !@route.railway_stations.any? %>
</p>

<%= link_to 'Редактировать', edit_admin_route_path(@route) %> |
<%= link_to 'Назад', admin_routes_path %>
